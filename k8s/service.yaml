apiVersion: v1
kind: Service
metadata:
  name: iris-api-svc
  labels:
    app: iris-api
spec:
  type: LoadBalancer
  selector:
    app: iris-api
  ports:
    - name: http
      port: 80          # external LB port
      targetPort: 8080  # containerPort
